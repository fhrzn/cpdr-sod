[project]
name = "cpdr-sod"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.11.0",
    "numpy>=2.4.6",
    "tqdm>=4.68.2",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
